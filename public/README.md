# Public Assets Folder for SPROUT & KERNEL

Place your static images, logos, and icons in this folder.
They will be served directly at root path in the browser.

Examples:
- `/public/logo.png` -> accessible via `/logo.png`
- `/public/favicon.ico` -> accessible via `/favicon.ico`
